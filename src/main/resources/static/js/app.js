// success
document.addEventListener("DOMContentLoaded", function () {
    const toastEl = document.getElementById('successToast');
    if (toastEl) {
        const toast = new bootstrap.Toast(toastEl);
        toast.show();

    }
});

// delete student
document.addEventListener("DOMContentLoaded", function () {
    const toastEl = document.getElementById('deleteToastS');
    if (toastEl) {
        const toast = new bootstrap.Toast(toastEl);
        toast.show();

    }
});

// delete book
document.addEventListener("DOMContentLoaded", function () {
    const toastEl = document.getElementById('deleteToastB');
    if (toastEl) {
        const toast = new bootstrap.Toast(toastEl);
        toast.show();

    }
});

// edit book
document.addEventListener("DOMContentLoaded", function () {
    const toastEl = document.getElementById('editToast');
    if (toastEl) {
        const toast = new bootstrap.Toast(toastEl);
        toast.show();

    }
});

// give book
document.addEventListener("DOMContentLoaded", function () {
    const toastEl = document.getElementById('giveToast');
    if (toastEl) {
        const toast = new bootstrap.Toast(toastEl);
        toast.show();

    }
});

// return book
document.addEventListener("DOMContentLoaded", function () {
    const toastEl = document.getElementById('returnToast');
    if (toastEl) {
        const toast = new bootstrap.Toast(toastEl);
        toast.show();
    }
});